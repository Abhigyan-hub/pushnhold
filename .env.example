PORT=4000
HOST=0.0.0.0
# RDS — use the master user you set in the AWS console. Do not commit real passwords.
DATABASE_URL=cascade.c16a8w8a40zh.eu-north-1.rds.amazonaws.com
# RDS requires SSL. Leave unset (default on). Only set false for local Postgres.
# DATABASE_SSL=false

JWT_SECRET=generate-a-long-random-string
JWT_EXPIRES_IN=7d

# Local Vite app (comma-separated if you also use 127.0.0.1)
FRONTEND_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

RAZORPAY_KEY_ID=rzp_live_TV8rU8aD7lA3h7
RAZORPAY_KEY_SECRET=fSYcEl8JaqEKNgIVLAPXjQGm
RAZORPAY_WEBHOOK_SECRET=cascade-wine-gamma.vercel.app2

# Match the region of your RDS + S3 bucket
AWS_REGION=eu-north-1
S3_BUCKET=cascade-event-images
# Prefer an EC2 instance IAM role. Keys only if you are not using a role:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# S3_PUBLIC_BASE_URL=https://cascade-event-images.s3.eu-north-1.amazonaws.com
